<h1>TOP PAGE</h1>
<% @posts.each do |post| %>
  <%# <%= post.content %>
  <%# <%= post.created_at %>
<div class="post">
    <div class="post-date">
      投稿日時：<%= post.created_at %>
    </div>
    <div class="post-content">
      <%= post.content %>
    </div>
  </div>
<% end %>